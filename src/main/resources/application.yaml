server:
  port: 9002

spring:
  application:
    name: hotel-service2025
  datasource:
    url: jdbc:mysql://localhost:3306/microservices2025
    username: root
    password: 2736@Holy
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka